import React from 'react';
import { Conainer, LoadingIndicator } from './styles';

export default function Loading() {
  return (
    <Conainer>
      <LoadingIndicator />
    </Conainer>
  );
}
